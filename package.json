{
  "name": "emergencysocialnetwork",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "mongod --dbpath data --fork --logpath /dev/null",
    "start": "node ./bin/www",
    "poststop": "mongo admin --eval 'db.shutdownServer()' > /dev/null",
    "pretest": "mongod --dbpath data --fork --logpath /dev/null",
    "test": "mocha test",
    "posttest": "mongo admin --eval 'db.shutdownServer()' > /dev/null"
    "postinstall": "bower install"
  },
  "dependencies": {
    "body-parser": "~1.15.2",
    "cookie-parser": "~1.4.3",
    "debug": "~2.2.0",
    "ejs": "~2.5.2",
    "express": "~4.14.0",
    "md5": "^2.2.1",
    "mongodb": "^2.2.21",
    "morgan": "~1.7.0",
    "serve-favicon": "~2.3.0",
    "socket.io": "^1.7.2",
    "bower": "1.8.0"

  }
}
