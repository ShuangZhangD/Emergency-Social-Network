# Language setting
language: node_js

# Version number
node_js:
  - "4.5.0"

services:
  - mongodb

before_install:
  - npm install
  - bower --allow-root install

before_script:
 - npm install
 - mkdir -p shippable/testresults
 - mkdir -p shippable/codecoverage


# after_success:
